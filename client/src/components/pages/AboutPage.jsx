import Navbar from '../Navbar';
import AboutSection from '../sections/AboutSection';

const AboutPage = () => {
    return (
        <div>
            <Navbar />
            <AboutSection />
        </div>
    );
};

export default AboutPage;