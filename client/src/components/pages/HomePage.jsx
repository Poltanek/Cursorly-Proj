import Navbar from '../Navbar';
import HeroSection from '../sections/HeroSection';

const HomePage = () => {
    return (
        <div>
            <Navbar />
            <HeroSection />
        </div>
    );
};

export default HomePage;